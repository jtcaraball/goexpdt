# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Ignore experiment output and input content
cmd/experiment/outputs/*
!cmd/experiment/output/.include
cmd/experiment/inputs/*
!cmd/experiment/inputs/.include
!cmd/experiment/inputs/mnist_d0_n400.json
!cmd/experiment/inputs/mnist_d0_input.txt
